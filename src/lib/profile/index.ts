export {
  buildVolumeProfile,
  DEFAULT_BUCKET_COUNT,
  DEFAULT_VALUE_AREA_SHARE,
  type ProfileOptions,
} from './volumeProfile';
export type { ProfileBar, ProfileBucket, ValueArea, VolumeProfile } from './types';
